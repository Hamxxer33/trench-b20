[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.26"
optimizer = true
optimizer_runs = 800
evm_version = "cancun"
via_ir = true
fs_permissions = [{ access = "read-write", path = "./" }]

[rpc_endpoints]
base = "${RPC_URL}"
